# 🐛 信源多样性问题：多条新闻来自同一信息源

## 问题现象

日报前 6 条新闻全部引用同一个信息源 —— 一个名为 **"AI News in a Minute"** 的 YouTube 短视频（约 2:30 的每日速报）。

- 内容正确，但 6 条新闻共用一个简短速报，导致：
  - 搜索广度下降（本应多源交叉，实际是单源）
  - 信息密度低（2:30 视频撑不起 6 条独立新闻的细节）
  - 读者观感差（像"翻译了一个视频"而非"综合多源报道"）

## 根因分析

### 1. 搜索过窄（根本原因）
- 此前为省 credit，把 Round 1 从 9 条压到 6 条、Round 2 从 20 压到 10 条
- 但实测一版 Tavily 仅消耗 16 credit，说明 credit 并非瓶颈
- 结果集太小时，覆盖面广的综述频道对任何 query 都"相关"，自然霸榜

### 2. `include_raw_content=True` 放大问题
- 该参数会把视频的完整文字稿抓回来
- 综述视频文字稿信息量大，DeepSeek 视其为"权威单源"，放心地用它撑起多条新闻
- 视频搜索轮应关闭该参数，仅新闻正文轮开启

### 3. 视频搜索变体单一
- 当前仅 `"{标题} podcast"` 和 `"{标题} news"` 两种变体
- 所有 query 命中的频道高度重叠，天然倾向返回同一个综述视频

## 修复方向（拓宽搜索，而非约束输出）

### A. 恢复搜索广度
- Round 1 回到 9-10 条多样化 query
- Round 2 视频候选从 5-8 扩到 10+
- 用足 credit 余量（当前 16 vs 1000 预算，空间巨大）

### B. 视频搜索按新闻类型分路
不同板块用不同搜索变体，命中不同频道，天然提高多样性：
- 技术新闻 → `explained` / `breakdown`
- 资本新闻 → `analysis` / `impact`
- 政策新闻 → `explainer` / `reaction`

### C. `include_raw_content` 分轮控制
- Round 1（新闻正文轮）：开启，用于提取日期锚点
- Round 2（视频搜索轮）：关闭，避免综述视频文字稿污染素材池

### D. prompt 只加软偏好（不做硬约束）
- 加一句"尽量为不同新闻匹配不同频道的视频"
- 不加"同一视频只能用一次"这类容易误伤的硬规则

## 核心原则

用足够广、足够多样的输入喂给 DeepSeek，让多样性自然涌现，而不是靠最后一层 prompt 死掐。

## 影响范围
- `main.py` — Round 1 query 数量、Round 2 搜索策略、`include_raw_content` 分轮控制
- 不改定时、成本、双层结构

## 优先级
P1（影响日报核心质量）
